/* 自动生成 — tools/gen_icon.py, 勿手改。
 * pet.ico: U盘模式磁盘图标 (Windows 经 autorun.inf ICON= 显示);
 * 空数组 = 不设置图标 (系统默认)。
 * 自定义: 放 tools/pet.ico 后重新构建 (build.bat 自动重跑本脚本) */
#ifndef USB_ICON_H
#define USB_ICON_H
#include <stdint.h>
#include <stddef.h>
static const uint8_t usb_icon_ico[] = {
    0x00,

};
static const size_t usb_icon_ico_len = 0;
#endif
